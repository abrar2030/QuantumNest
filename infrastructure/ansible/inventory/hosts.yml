---
all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
    deploy_environment: "{{ env | default('dev') }}"

  children:
    webservers:
      hosts: {}
      vars:
        nginx_port: 443

    databases:
      hosts: {}
      vars:
        postgres_port: 5432

    monitoring:
      hosts: {}
